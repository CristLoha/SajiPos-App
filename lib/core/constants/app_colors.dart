import 'package:flutter/material.dart';

class AppColors {
  // Primary Palette - Modern Deep Indigo
  static const Color primary = Color(0xFF1A1D3B);       // Deep navy for sidebar & headers
  static const Color accent = Color(0xFF4361EE);         // Vivid blue for CTA buttons & highlights
  static const Color accentLight = Color(0xFFEEF1FF);    // Very subtle blue tint

  // Background
  static const Color background = Color(0xFFF5F6FA);     // Warm off-white
  static const Color backgroundLight = Color(0xFFF5F6FA);// Alias
  static const Color cardBackground = Colors.white;
  static const Color white = Colors.white;

  // Text Colors
  static const Color textPrimary = Color(0xFF1E1E2D);    // Near-black
  static const Color textSecondary = Color(0xFF9CA3AF);  // Muted grey

  // Borders & Dividers
  static const Color border = Color(0xFFEBEDF3);         // Very light grey

  // Semantic
  static const Color success = Color(0xFF22C55E);        // Green
  static const Color danger = Color(0xFFEF4444);         // Red
}
